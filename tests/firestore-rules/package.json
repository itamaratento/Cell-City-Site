{
  "name": "cellcity-firestore-rules-tests",
  "private": true,
  "description": "Testes de Firestore Rules.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --only firestore \"node --test --test-concurrency=1\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
