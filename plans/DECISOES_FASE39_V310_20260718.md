# FASE 3.9 — DECISÕES TÉCNICAS OFICIAIS — RELEASE v3.1.0

> Registro literal das decisões emitidas pelo dono do projeto em 2026-07-18,
> em resposta ao relatório `plans/FASE39_ENCERRAMENTO_V310_MODELO_DADOS_CICD_20260718.md`.
> Status da release: 🟡 APROVADA COM RESSALVAS. Próximo marco: FASE 4.2 —
> RECERTIFICAÇÃO FINAL E ENCERRAMENTO OPERACIONAL.

- **D01 — Padrão oficial do modelo de dados (APROVADO):** Português, camelCase;
  `empresa_id` mantido; `criadoEm`/`atualizadoEm`. Campos legados EN permanecem
  suportados. Sem migração em massa nesta release. Estratégia: Expand → Compatibilidade → Contract.
- **D02 — Dados legados (APROVADO):** `os`, `clientes` e demais coleções
  históricas preservadas; migração somente em janela exclusiva.
- **D03 — CI/CD (APROVADO):** pipeline oficial via **Workload Identity
  Federation**; eliminar SA JSON e `FIREBASE_SA_KEY`.
- **D04 — GitHub Actions:** autorizada a correção do workflow da main
  (paridade com develop, deploy reproduzível, validação automática, smoke via pipeline).
- **D05 — Backup semanal (prioridade ALTA):** corrigir imediatamente workflow,
  cron, autenticação e permissões. Nenhum ciclo operacional inicia com backup falhando.
- **D06 — Firestore Indexes:** antes da promoção — exportar prod, comparar
  develop e main, atualizar firestore.indexes.json, validar, commitar. Zero drift.
- **D07 — Catálogo público:** validar `produtos` × `catalogo_produtos`;
  se houver produtos ativos não publicados, corrigir sincronização.
- **D08 — Deploy:** após etapas anteriores, deployar Rules/Storage/Indexes/Functions
  exclusivamente pelo pipeline oficial; deploy manual vira exceção operacional.
- **D09 — Homologação:** smoke autenticado, Portal, Portal Público, RBAC,
  Storage, Cloud Functions, Dashboard, CRM, OS — com evidências.
- **D10 — Promoção develop→main** somente com: pipeline verde, deploy via CI,
  backup funcional, índices sincronizados, smoke aprovado.
- **D11 — Certificação:** permanece 🟡; eleva a 🟢 somente após drift zero,
  deploy CI validado, backup operacional, WIF configurado, smoke final e evidências.

Aprovado: padrão PT camelCase, Expand–Contract, WIF, correção do CI, sincronização
de índices, validação do catálogo, correção do backup. Adiado: migração completa
do modelo e conversão dos campos legados EN.
