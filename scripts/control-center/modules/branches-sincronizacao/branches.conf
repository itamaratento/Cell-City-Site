# Cell City Control Center — Branches e Sincronização: configurações do
# módulo (Fase 5). Editável via menu (Configurações) ou na mão — formato
# KEY="VALUE", mesmo padrão de scripts/control-center/config/control-center.conf.
# Nada aqui altera configuração global do Git nem do Control Center.
REMOTE_PADRAO="origin"
BRANCH_PRINCIPAL="main"
BRANCH_DESENVOLVIMENTO="develop"
VERBOSIDADE="normal"
LOG_SINCRONIZACAO="on"
TIMEOUT_REDE="15"
