# Cell City Control Center — metadados da Fase atual.
# Lido por core/menu.sh para exibir a Fase no cabeçalho do menu.
# A versão (semver) NÃO mora mais aqui desde a Fase 1.1 — vive em
# ../VERSION, fonte única (evita duas cópias divergindo, mesmo risco já
# visto em CRM/firestore.rules vs. firestore.rules na raiz).
# Atualizar CC_FASE a cada Sprint que muda a Fase do Roadmap (README.md).
CC_FASE="1"
